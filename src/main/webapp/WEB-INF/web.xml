<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
        This file tells Tomcat how to set up the Jersey JAX-RS servlet.
        Without this, Tomcat doesn't know where to find our API endpoints.
    -->

    <servlet>
        <servlet-name>jersey-servlet</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
        <!-- Tell Jersey to scan our package for @Path, @Provider etc. -->
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>com.smartcampus</param-value>
        </init-param>
        <init-param>
            <param-name>jakarta.ws.rs.Application</param-name>
            <param-value>com.smartcampus.application.SmartCampusApplication</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>jersey-servlet</servlet-name>
        <!-- Map all requests under /api/v1/ to Jersey -->
        <url-pattern>/api/v1/*</url-pattern>
    </servlet-mapping>

</web-app>
